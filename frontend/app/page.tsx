"use client";

import { useEffect, useState } from "react";
import type { FormEvent, MouseEvent } from "react";
import { ArrowLeft, ArrowRight, CalendarClock, CheckCircle2, Database, Eye, FileText, Play, Plus, Search, ShieldCheck, Table2 } from "lucide-react";
import { AppShell } from "../components/AppShell";
import { Button, Card, DataTable, EmptyState, LoadingState, MetricCard, PageHeader, StatusBadge, Stepper } from "../components/ui";

const API = process.env.NEXT_PUBLIC_API_URL || "http://127.0.0.1:8000";
type Source = { id: string; name: string; description: string; source_type: string; status: string; created_at: string };
type Preview = { columns: string[]; preview: Record<string, string | null>[]; rows_estimated: number | null; inferred_types: Record<string, string> };
type Run = { id: string; started_at: string; finished_at?: string; status: string; rows_read: number; rows_inserted: number; rows_updated: number; rows_unchanged: number; rows_rejected: number; error_count: number; warning_count: number };
type WizardRule = { column_name: string; rule_type: string; severity: string; rule_config: Record<string, unknown> };
type WizardOptions = { reader_options: Record<string, unknown>; schema_name: string; table_name: string; missing_record_behavior: string; batch_size: number; validation_rules: WizardRule[]; schedule: { enabled: boolean; schedule_type: string; timezone: string; hour: number; minute: number; interval_hours: number; cron_expression: string } };
type RunDetail = Run & { source_id: string; source_name?: string; finished_at?: string; watermark_before?: string; watermark_after?: string; configuration_snapshot: Record<string, unknown>; errors: { id: string; type: string; message: string; created_at: string }[]; exceptions: Record<string, unknown>[] };
const initialWizardOptions: WizardOptions = { reader_options: { delimiter: "", header_row: 1, preview_rows: 10, record_tag: "", sheet: "" }, schema_name: "data", table_name: "records", missing_record_behavior: "IGNORE", batch_size: 5000, validation_rules: [], schedule: { enabled: false, schedule_type: "DAILY", timezone: "America/Santo_Domingo", hour: 2, minute: 0, interval_hours: 6, cron_expression: "" } };
type Column = { column_name: string; data_type: string; is_nullable: string; ordinal_position?: number };
type TableObject = { table_name: string; table_type: string };

export default function Home() {
  const [active, setActive] = useState("Fuentes de datos");
  const [sources, setSources] = useState<Source[]>([]);
  const [selected, setSelected] = useState<Source | null>(null);
  const [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState<Preview | null>(null);
  const [runs, setRuns] = useState<Run[]>([]);
  const [exceptions, setExceptions] = useState<Record<string, unknown>[]>([]);
  const [schedules, setSchedules] = useState<Record<string, unknown>[]>([]);
  const [message, setMessage] = useState("");
  const [busy, setBusy] = useState("");
  const [showWizard, setShowWizard] = useState(false);
  const [wizardOptions, setWizardOptions] = useState<WizardOptions>(initialWizardOptions);
  const [runDetail, setRunDetail] = useState<RunDetail | null>(null);
  const [rule, setRule] = useState({ column_name: "Amount", rule_type: "NUMERIC", severity: "ERROR" });
  const [form, setForm] = useState({ name: "", description: "", source_type: "FILE", dataset_name: "transactions", table_name: "transactions", load_mode: "INCREMENTAL_UPSERT", business_key: "ID" });
  const [schedule, setSchedule] = useState({ data_source_id: "", schedule_type: "DAILY", timezone: "America/Santo_Domingo", hour: 2, minute: 0, interval_hours: 6, cron_expression: "" });
  const [connection, setConnection] = useState({ name: "", engine: "POSTGRESQL", host: "127.0.0.1", port: "5432", database: "kiriox", username: "kiriox", password: "", schema: "public" });
  const [connectionId, setConnectionId] = useState("");

  const loadSources = () => fetch(`${API}/api/sources`).then(response => response.json()).then(setSources).catch(() => setMessage("No pudimos conectar con el servicio local."));
  useEffect(() => { void loadSources(); }, []);
  useEffect(() => { if (active === "Automatizaciones") void fetch(`${API}/api/schedules`).then(response => response.json()).then(setSchedules).catch(() => setMessage("No pudimos conectar con el servicio local.")); }, [active]);

  async function createSource(event: FormEvent, options: WizardOptions = wizardOptions, sourceFile: File | null = null) {
    event.preventDefault(); setBusy("create-source");
    let response: Response;
    try { response = await fetch(`${API}/api/sources`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, table_name: options.table_name || form.table_name, business_key: form.business_key.split(",").map(value => value.trim()).filter(Boolean), config: { reader_options: options.reader_options, schema_name: options.schema_name, missing_record_behavior: options.missing_record_behavior, batch_size: options.batch_size } }) }); }
    catch { setBusy(""); setMessage("No pudimos conectar con el servicio local."); return; }
    if (!response.ok) { setBusy(""); setMessage("No pudimos guardar la fuente. Revisa los datos e inténtalo de nuevo."); return; }
    const created = await response.json();
    const ruleResponses = await Promise.all(options.validation_rules.map(rule => fetch(`${API}/api/sources/${created.id}/rules`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(rule) })));
    let scheduleMessage = "";
    if (options.schedule.enabled) {
      const scheduleResponse = await fetch(`${API}/api/schedules`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...options.schedule, data_source_id: created.id, retry_count: 3, retry_delay_seconds: 300 }) });
      scheduleMessage = scheduleResponse.ok ? " La revisión automática quedó programada." : " La automatización quedó pendiente de revisar.";
    }
    let runMessage = "";
    if (sourceFile) {
      const upload = new FormData(); upload.append("file", sourceFile);
      const runResponse = await fetch(`${API}/api/sources/${created.id}/runs`, { method: "POST", body: upload });
      if (runResponse.ok) { const result = await runResponse.json(); runMessage = ` Se revisaron ${result.rows_read ?? 0} registros y ${result.rows_rejected ?? 0} quedaron por revisar.`; }
      else runMessage = " La fuente quedó creada; podrás procesar el archivo desde su ficha.";
    }
    setBusy(""); setMessage(`Fuente creada correctamente.${runMessage}${scheduleMessage}`); setShowWizard(false); setWizardOptions(initialWizardOptions); setForm({ ...form, name: "", description: "" }); await loadSources();
  }

  async function inspect() { if (!selected || !file) return; setBusy("preview"); const formData = new FormData(); formData.append("file", file); const response = await fetch(`${API}/api/sources/${selected.id}/preview`, { method: "POST", body: formData }); setBusy(""); if (response.ok) setPreview(await response.json()); else setMessage(await response.text()); }
  async function runFile() { if (!selected || !file) return; setBusy("run"); const formData = new FormData(); formData.append("file", file); const response = await fetch(`${API}/api/sources/${selected.id}/runs`, { method: "POST", body: formData }); const result = await response.json(); setBusy(""); setMessage(`${result.status}: ${result.rows_read ?? 0} recibidos · ${result.rows_inserted ?? 0} nuevos · ${result.rows_updated ?? 0} actualizados · ${result.rows_rejected ?? 0} rechazados`); await loadHistory(selected.id); }
  async function runDatabase() { if (!selected) return; setBusy("run"); const response = await fetch(`${API}/api/sources/${selected.id}/db-runs`, { method: "POST" }); const result = await response.json(); setBusy(""); setMessage(response.ok ? `${result.status}: ${result.rows_read ?? 0} leídos · ${result.rows_inserted ?? 0} nuevos · ${result.rows_updated ?? 0} actualizados · watermark ${result.watermark_after || "sin cambios"}` : await response.text()); await loadHistory(selected.id); }
  async function addRule() { if (!selected) return; const response = await fetch(`${API}/api/sources/${selected.id}/rules`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...rule, rule_config: {} }) }); setMessage(response.ok ? "Regla guardada en la configuración" : await response.text()); }
  async function loadHistory(sourceId: string) { const response = await fetch(`${API}/api/sources/${sourceId}/runs`); if (response.ok) setRuns(await response.json()); }
  async function loadRunDetail(run: Run) { const response = await fetch(`${API}/api/runs/${run.id}`); if (response.ok) setRunDetail(await response.json()); else setMessage(await response.text()); }
  async function createConnection(event: FormEvent) { event.preventDefault(); setBusy("connection"); const response = await fetch(`${API}/api/connections`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...connection, port: Number(connection.port) }) }); if (!response.ok) { setBusy(""); setMessage(await response.text()); return; } const created = await response.json(); const test = await fetch(`${API}/api/connections/${created.id}/test`, { method: "POST" }); setBusy(""); if (!test.ok) { setMessage(await test.text()); return; } setConnectionId(created.id); setMessage(`Conexión ${connection.engine} verificada y lista para explorar`); }
  async function loadExceptions(run: Run) { const response = await fetch(`${API}/api/runs/${run.id}/exceptions`); if (response.ok) { setExceptions(await response.json()); setActive("Datos por revisar"); } }
  async function createSchedule(event: FormEvent) { event.preventDefault(); const response = await fetch(`${API}/api/schedules`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...schedule, hour: Number(schedule.hour), minute: Number(schedule.minute), interval_hours: Number(schedule.interval_hours), retry_count: 3, retry_delay_seconds: 300 }) }); setMessage(response.ok ? "Automatización guardada" : await response.text()); if (response.ok) setSchedules([...schedules, await response.json()]); }
  async function toggleSchedule(scheduleId: string) { const response = await fetch(`${API}/api/schedules/${scheduleId}/toggle`, { method: "POST" }); if (!response.ok) { setMessage(await response.text()); return; } const result = await response.json(); setSchedules(schedules.map(item => String(item.id) === scheduleId ? { ...item, enabled: result.enabled } : item)); setMessage(result.enabled ? "Automatización activada" : "Automatización pausada"); }
  const selectSource = (source: Source) => { setSelected(source); setPreview(null); setFile(null); setRunDetail(null); setExceptions([]); void loadHistory(source.id); };
  const activeSources = sources.filter(source => source.status === "ACTIVE").length;
  const dbSources = sources.filter(source => source.source_type === "POSTGRESQL").length;

  return <AppShell active={active} sourceCount={sources.length} onNavigate={setActive}>
    <header className="global-header"><div className="system-status"><span className="status-dot" /><div><strong>PostgreSQL</strong><small>Local · Connected</small></div><span className="system-address">127.0.0.1:5432</span></div></header>
    {active === "Fuentes de datos" && <SourcesView sources={sources} selected={selected} file={file} preview={preview} form={form} setForm={setForm} setFile={setFile} selectSource={selectSource} inspect={inspect} runFile={runFile} runDatabase={runDatabase} createSource={createSource} rule={rule} setRule={setRule} addRule={addRule} showWizard={showWizard} setShowWizard={setShowWizard} busy={busy} activeSources={activeSources} dbSources={dbSources} wizardOptions={wizardOptions} setWizardOptions={setWizardOptions} />}
    {active === "Conectar una base de datos" && <><div className="engine-selector"><label>Tipo de base<select value={connection.engine} onChange={event => setConnection({ ...connection, engine: event.target.value })}><option>POSTGRESQL</option><option>SQLSERVER</option><option>MYSQL</option><option>MARIADB</option><option>ORACLE</option><option>DB2</option></select></label></div><ConnectionView connection={connection} setConnection={setConnection} createConnection={createConnection} connectionId={connectionId} onMessage={setMessage} onSourceCreated={loadSources} busy={busy} /></>}
    {active === "Actividad" && <HistoryView sources={sources} runs={runs} selected={selected} selectSource={selectSource} loadExceptions={loadExceptions} loadRunDetail={loadRunDetail} runDetail={runDetail} clearRunDetail={() => setRunDetail(null)} />}
    {active === "Datos por revisar" && <ExceptionsView exceptions={exceptions} />}
    {active === "Automatizaciones" && <ScheduleView sources={sources} schedule={schedule} setSchedule={setSchedule} createSchedule={createSchedule} schedules={schedules} toggleSchedule={toggleSchedule} />}
    {active === "Preferencias" && <Card className="settings-card"><PageHeader eyebrow="TU ESPACIO LOCAL" title="Preferencias" description="Consulta cómo trabaja esta instalación. No necesitas cambiar nada para comenzar." /><div className="settings-list"><div><b>Hora local</b><span>America/Santo_Domingo</span></div><div><b>Procesamiento</b><span>En lotes de hasta 5,000 registros</span></div><div><b>Privacidad</b><span>Los datos permanecen en este equipo</span></div></div></Card>}
    {message && <div className="toast" role="status"><CheckCircle2 size={16} /> {message}</div>}
  </AppShell>;
}

function SourcesView({ sources, selected, file, preview, form, setForm, setFile, selectSource, inspect, runFile, runDatabase, createSource, rule, setRule, addRule, showWizard, setShowWizard, busy, activeSources, dbSources, wizardOptions, setWizardOptions }: any) {
  if (showWizard) return <SourceWizard form={form} setForm={setForm} createSource={createSource} close={() => setShowWizard(false)} busy={busy} options={wizardOptions} setOptions={setWizardOptions} />;
  return <><PageHeader eyebrow="CUIDA TUS DATOS" title="Fuentes de datos" description="Conecta un archivo, revisa su calidad y conserva un registro claro de cada resultado." action={<Button onClick={() => setShowWizard(true)} icon={<Plus size={17} aria-hidden="true" />}>Agregar fuente</Button>} /><div className="summary-grid"><MetricCard label="Fuentes activas" value={activeSources} note="listas para trabajar" /><MetricCard label="Fuentes de base de datos" value={dbSources} note="conexiones de solo lectura" tone="primary" /><MetricCard label="Última actividad" value="Hoy" note="en este equipo" tone="orange" /></div><Card className="sources-card"><div className="section-heading"><div><span className="section-kicker">TUS ORÍGENES</span><h2>Fuentes configuradas</h2><p>Elige una fuente para revisar el archivo, ver el resultado o volver a procesarla.</p></div><span className="count-chip">{sources.length.toString().padStart(2, "0")} FUENTES</span></div>{sources.length ? <DataTable><div className="data-row data-head"><span>Nombre</span><span>Origen</span><span>Estado</span><span>Última actividad</span><span /></div>{sources.map((source: Source) => <button aria-label={`Abrir fuente ${source.name}`} aria-current={selected?.id === source.id ? "true" : undefined} className={`data-row data-button ${selected?.id === source.id ? "selected" : ""}`} key={source.id} onClick={() => selectSource(source)}><span className="source-name" title={source.name}><b>{source.name}</b><small>{source.description || "Sin descripción"}</small></span><span><i className="type-icon"><Database size={14} /></i>{source.source_type === "FILE" ? "Archivo" : "Base de datos"}</span><span><StatusBadge status={source.status} /></span><span className="muted numeric">Aún no procesada</span><span className="row-arrow"><ArrowRight size={16} aria-hidden="true" /></span></button>)}</DataTable> : <EmptyState title="Todavía no tienes fuentes" description="Agrega tu primer archivo para comenzar a revisar y cuidar tus datos." action={<Button onClick={() => setShowWizard(true)}>Agregar mi primera fuente</Button>} />}</Card>{selected && <SourceDetail selected={selected} file={file} preview={preview} setFile={setFile} inspect={inspect} runFile={runFile} runDatabase={runDatabase} rule={rule} setRule={setRule} addRule={addRule} busy={busy} />}</>;
}

function SourceWizard({ form, setForm, createSource, close, busy, options, setOptions }: any) {
  const steps = ["Define tu fuente", "Elige tus datos", "Confirma y activa"];
  const [step, setStep] = useState(0);
  const [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState<Preview | null>(null);
  const [previewBusy, setPreviewBusy] = useState(false);
  const [error, setError] = useState("");
  const [autoRun, setAutoRun] = useState(Boolean(options.schedule.enabled));
  const [qualityChecks, setQualityChecks] = useState(options.validation_rules.length > 0);
  const updateReader = (key: string, value: string | number) => setOptions({ ...options, reader_options: { ...options.reader_options, [key]: value } });
  const updateSchedule = (key: string, value: string | number | boolean) => setOptions({ ...options, schedule: { ...options.schedule, [key]: value } });
  async function loadPreview() {
    if (!file) { setError("Primero elige un archivo."); return; }
    setPreviewBusy(true); setError("");
    const payload = new FormData(); payload.append("file", file); payload.append("reader_options", JSON.stringify(options.reader_options));
    const response = await fetch(`${API}/api/preview`, { method: "POST", body: payload });
    setPreviewBusy(false);
    if (response.ok) {
      const nextPreview = await response.json(); setPreview(nextPreview);
      if (qualityChecks && nextPreview.columns.includes("Amount")) setOptions({ ...options, validation_rules: [{ column_name: "Amount", rule_type: "NUMERIC", severity: "ERROR", rule_config: {} }] });
    } else setError("No pudimos revisar este archivo. Comprueba que el formato sea válido.");
  }
  function toggleQuality(enabled: boolean) {
    setQualityChecks(enabled);
    setOptions({ ...options, validation_rules: enabled && preview?.columns.includes("Amount") ? [{ column_name: "Amount", rule_type: "NUMERIC", severity: "ERROR", rule_config: {} }] : [] });
  }
  function nextStep(event: MouseEvent<HTMLButtonElement>) {
    event.preventDefault();
    if (step === 0 && !form.name.trim()) { setError("Escribe un nombre para identificar la fuente."); return; }
    if (step === 1 && !file) { setError("Elige el archivo que quieres cuidar."); return; }
    setError(""); setStep(Math.min(steps.length - 1, step + 1));
  }
  const stepHelp = ["Dinos qué información vas a cuidar.", "Elige un archivo y revisa una muestra antes de guardarlo.", "Confirma las opciones importantes; nosotros nos encargamos de lo técnico."][step];
  return <div className="wizard-page"><button className="back-link" onClick={close} type="button"><ArrowLeft size={15} /> Volver a fuentes</button><PageHeader eyebrow={`CONFIGURACIÓN SIMPLE · ${String(step + 1).padStart(2, "0")} / 03`} title="Agregar una fuente" description="Solo necesitamos tres decisiones. El resto lo preparamos por ti." /><Card className="wizard-card"><div className="wizard-topline"><span>PROGRESO</span><strong>{String(step + 1).padStart(2, "0")} <i /> 03</strong></div><Stepper steps={steps} current={step} /><div className="wizard-content"><aside className="wizard-index">{steps.map((item, index) => <div className={index === step ? "active" : index < step ? "done" : ""} key={item}><span>{String(index + 1).padStart(2, "0")}</span>{item}</div>)}</aside><form className="wizard-form" onSubmit={(event) => createSource(event, options, file)}><div className="form-intro"><div className="form-icon"><FileText size={20} /></div><div><h2>{steps[step]}</h2><p>{stepHelp}</p></div></div>
          {step === 0 && <><label>Nombre de la fuente<input required placeholder="Ej. Ventas de esta semana" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /><small>Un nombre corto para reconocerla después.</small></label><label>¿Qué datos vas a cuidar?<textarea placeholder="Ej. Ventas, clientes o inventario de la tienda" value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /><small>Cuéntanos para qué sirve esta información.</small></label><div className="friendly-choice"><b>¿De dónde vienen los datos?</b><p>Elige Archivo y después selecciona el archivo en el siguiente paso.</p><button type="button" className="choice-chip choice-chip-button" aria-label="Elegir tipo de archivo" onClick={() => { setError(""); setStep(1); }}><FileText size={14} /> Archivo</button></div></>}
          {step === 1 && <><label>Elige el archivo<input type="file" accept=".csv,.txt,.xlsx,.xls,.xml" onChange={(event) => { setFile(event.target.files?.[0] || null); setPreview(null); }} /><small>Formatos aceptados: CSV, TXT, XLSX, XLS o XML.</small></label>{file && <p className="field-note">Archivo listo: <b>{file.name}</b></p>}<div className="preview-callout"><div><b>Revisa una muestra</b><p>Veremos algunas filas para comprobar que todo se entiende antes de guardar.</p></div><Button type="button" variant="secondary" onClick={() => void loadPreview()} disabled={!file || previewBusy} icon={<Eye size={15} />}>{previewBusy ? "Revisando…" : "Revisar archivo"}</Button></div>{preview ? <div className="simple-preview"><b>La muestra está lista</b><p>{preview.columns.slice(0, 6).join(" · ")}</p><small>Mostramos solo una muestra; el archivo original no se modifica.</small></div> : <p className="field-note">Revisar el archivo es opcional, pero te ayuda a detectar problemas antes de procesarlo.</p>}<details className="advanced-options"><summary>Opciones avanzadas del archivo</summary><div className="form-grid"><label>Separador<input value={String(options.reader_options.delimiter || "")} placeholder="," onChange={(event) => updateReader("delimiter", event.target.value)} /></label><label>Fila con títulos<input type="number" min="1" value={Number(options.reader_options.header_row || 1)} onChange={(event) => updateReader("header_row", Number(event.target.value))} /></label></div><div className="form-grid"><label>Hoja de Excel<input value={String(options.reader_options.sheet || "")} placeholder="Primera hoja" onChange={(event) => updateReader("sheet", event.target.value)} /></label><label>Etiqueta XML<input value={String(options.reader_options.record_tag || "")} placeholder="transaction" onChange={(event) => updateReader("record_tag", event.target.value)} /></label></div></details></>}
          {step === 2 && <><div className="friendly-summary"><div><span>Fuente</span><b>{form.name || "Sin nombre"}</b></div><div><span>Archivo</span><b>{file?.name || "Sin archivo"}</b></div><div><span>Cómo cuidaremos los datos</span><b>Detectaremos cambios y evitaremos duplicados</b></div></div><label className="friendly-toggle"><input type="checkbox" checked={qualityChecks} onChange={(event) => toggleQuality(event.target.checked)} /> Revisar problemas comunes antes de guardar</label><label className="friendly-toggle"><input type="checkbox" checked={autoRun} onChange={(event) => { setAutoRun(event.target.checked); updateSchedule("enabled", event.target.checked); }} /> Revisar este archivo automáticamente</label>{autoRun && <div className="form-grid"><label>Cada cuánto<select value={options.schedule.schedule_type} onChange={(event) => updateSchedule("schedule_type", event.target.value)}><option value="DAILY">Cada día</option><option value="HOURLY">Cada hora</option><option value="WEEKLY">Cada semana</option><option value="MONTHLY">Cada mes</option></select></label><label>Hora<input type="number" min="0" max="23" value={options.schedule.hour} onChange={(event) => updateSchedule("hour", Number(event.target.value))} /></label></div>}<p className="field-note"><ShieldCheck size={15} /> Usaremos una configuración segura en segundo plano. No necesitas elegir tablas, lotes ni claves técnicas.</p></>}
          {error && <p className="form-error">{error}</p>}<div className="wizard-footer"><span><ShieldCheck size={16} /> Paso {step + 1} de 3 · Tú decides lo importante</span><div className="wizard-actions">{step > 0 && <Button type="button" variant="ghost" icon={<ArrowLeft size={15} />} onClick={() => { setError(""); setStep(step - 1); }}>Atrás</Button>}{step < steps.length - 1 ? <Button type="button" onClick={nextStep}>Continuar</Button> : <Button type="submit" disabled={busy === "create-source"}>{busy === "create-source" ? "Guardando…" : "Crear y revisar fuente"}</Button>}</div></div></form></div></Card></div>;
}

function SourceDetail({ selected, file, preview, setFile, inspect, runFile, runDatabase, rule, setRule, addRule, busy }: any) {
  const isDatabase = selected.source_type === "POSTGRESQL";
  return <Card className="source-detail"><div className="detail-header"><div><span className="section-kicker">SIGUIENTE PASO</span><h2>{selected.name}</h2><p>{isDatabase ? "Esta conexión está lista para consultar tus datos." : "Sube el archivo que quieres revisar y procesar."}</p></div><StatusBadge status={selected.status} /></div><div className="detail-content">{isDatabase ? <div className="db-operation"><div><span className="operation-icon"><Database size={18} /></span><div><b>Conexión lista</b><p>Consultaremos tus datos sin modificar la base original y guardaremos el resultado en este equipo.</p></div></div><Button onClick={runDatabase} disabled={busy === "run"} icon={<Play size={15} />}>{busy === "run" ? "Procesando…" : "Consultar datos"}</Button></div> : <div className="file-operation"><div className="file-drop"><FileText size={21} /><div><b>Sube el archivo de hoy</b><small>CSV, TXT, XLSX, XLS o XML</small></div><input aria-label="Elegir archivo para procesar" type="file" accept=".csv,.txt,.xlsx,.xls,.xml" onChange={(event) => setFile(event.target.files?.[0] || null)} /></div>{file && <p className="field-note">Archivo seleccionado: <b>{file.name}</b></p>}<div className="action-row"><Button variant="secondary" disabled={!file || busy === "preview"} onClick={inspect} icon={<Eye size={15} />}>{busy === "preview" ? "Revisando…" : "Revisar archivo"}</Button><Button disabled={!file || busy === "run"} onClick={runFile} icon={<Play size={15} />}>{busy === "run" ? "Procesando…" : "Procesar archivo"}</Button></div><p className="helper-text">Primero puedes revisar una muestra. Cuando estés listo, procesa el archivo para guardar los cambios.</p></div>}<details className="advanced-controls"><summary>Ver controles avanzados</summary><div className="rule-builder"><div><span className="section-kicker">REGLAS DE CALIDAD</span><p>Define una comprobación adicional para este archivo.</p></div><div className="rule-row"><select aria-label="Campo a revisar" value={rule.column_name} onChange={event => setRule({ ...rule, column_name: event.target.value })}><option>Amount</option><option>ID</option><option>TransactionDate</option></select><select aria-label="Tipo de comprobación" value={rule.rule_type} onChange={event => setRule({ ...rule, rule_type: event.target.value })}><option>NUMERIC</option><option>REQUIRED</option><option>DATE</option><option>INTEGER</option><option>MAX_LENGTH</option><option>MIN</option><option>MAX</option><option>REGEX</option><option>UNIQUE</option></select><select aria-label="Qué hacer si falla" value={rule.severity} onChange={event => setRule({ ...rule, severity: event.target.value })}><option>ERROR</option><option>WARNING</option></select><Button variant="secondary" onClick={addRule}>Guardar comprobación</Button></div></div></details>{preview && <PreviewPanel preview={preview} />}</div></Card>;
}

function PreviewPanel({ preview }: { preview: Preview }) { return <div className="preview-panel"><div className="preview-heading"><div><span className="section-kicker">MUESTRA DE DATOS</span><h3>Así se ve tu información</h3></div><span>{preview.columns.length} campos · muestra parcial</span></div><DataTable className="preview-table"><div className="data-row data-head">{preview.columns.slice(0, 5).map(column => <span key={column}>{column}</span>)}</div>{preview.preview.slice(0, 8).map((row, index) => <div className="data-row" key={index}>{preview.columns.slice(0, 5).map(column => <span key={column}>{row[column] ?? "—"}</span>)}</div>)}</DataTable></div>; }

function ConnectionView({ connection, setConnection, createConnection, connectionId, onMessage, onSourceCreated, busy }: any) {
  const [schemas, setSchemas] = useState<string[]>([]); const [schema, setSchema] = useState(connection.schema); const [tables, setTables] = useState<TableObject[]>([]); const [table, setTable] = useState(""); const [columns, setColumns] = useState<Column[]>([]); const [rows, setRows] = useState<Record<string, unknown>[]>([]); const [loading, setLoading] = useState(false);
  useEffect(() => { if (!connectionId) return; setLoading(true); fetch(`${API}/api/connections/${connectionId}/schemas`).then(response => response.json()).then(items => { const next = Array.isArray(items) ? items : []; setSchemas(next); setSchema(next.includes(connection.schema) ? connection.schema : next[0] || ""); }).catch(() => onMessage("No pudimos cargar los espacios de datos.")).finally(() => setLoading(false)); }, [connectionId, connection.schema, onMessage]);
  useEffect(() => { if (!connectionId || !schema) return; setLoading(true); setTable(""); setColumns([]); setRows([]); fetch(`${API}/api/connections/${connectionId}/objects?schema=${encodeURIComponent(schema)}`).then(response => response.json()).then(items => setTables(Array.isArray(items) ? items : [])).catch(() => onMessage("No pudimos cargar las tablas.")).finally(() => setLoading(false)); }, [connectionId, schema, onMessage]);
  async function inspectTable(nextTable: string) { setTable(nextTable); setRows([]); if (!nextTable || !connectionId) return; const response = await fetch(`${API}/api/connections/${connectionId}/columns?schema=${encodeURIComponent(schema)}&table=${encodeURIComponent(nextTable)}`); if (response.ok) setColumns(await response.json()); else onMessage(await response.text()); }
  async function previewTable() { if (!connectionId || !table) return; const response = await fetch(`${API}/api/connections/${connectionId}/preview?schema=${encodeURIComponent(schema)}&table=${encodeURIComponent(table)}&limit=25`); if (response.ok) setRows(await response.json()); else onMessage(await response.text()); }
  async function createDatabaseSource() { if (!connectionId || !schema || !table) return; const names = columns.map(column => column.column_name); const watermark = names.includes("updated_at") ? "updated_at" : ""; const primaryKey = names.includes("id") ? "id" : ""; const response = await fetch(`${API}/api/sources`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: `${connection.name || "PostgreSQL"} · ${table}`, description: `Tabla ${schema}.${table} en modo read-only`, source_type: "POSTGRESQL", connection_id: connectionId, config: { schema, table, watermark_column: watermark, watermark_primary_key: primaryKey, columns: Object.fromEntries(columns.map(column => [column.column_name, { data_type: column.data_type }])) }, dataset_name: table, table_name: table, load_mode: primaryKey ? "INCREMENTAL_UPSERT" : "APPEND", business_key: primaryKey ? [primaryKey] : [] }) }); onMessage(response.ok ? "Conexión agregada como fuente" : await response.text()); if (response.ok) onSourceCreated(); }
  return <><PageHeader eyebrow="CONECTA TUS DATOS" title="Conectar una base de datos" description="Conecta una vez y explora tus tablas sin escribir consultas." /><div className="explorer-layout"><Card className="connection-card"><div className="section-heading"><div><span className="section-kicker">PASO 1 · CONECTAR</span><h2>Datos de conexión</h2><p>Esta instalación guarda tus credenciales localmente y no modifica la base original.</p></div><span className="connection-badge"><ShieldCheck size={15} /> Protegida</span></div><form className="form-body" onSubmit={createConnection}><label>Nombre para identificarla<input required value={connection.name} placeholder="Mi base de ventas" onChange={event => setConnection({ ...connection, name: event.target.value })} /></label><div className="form-grid"><label>Dirección del servidor<input required value={connection.host} placeholder="127.0.0.1" onChange={event => setConnection({ ...connection, host: event.target.value })} /></label><label>Puerto<input type="number" value={connection.port} onChange={event => setConnection({ ...connection, port: event.target.value })} /></label></div><div className="form-grid"><label>Nombre de la base<input required value={connection.database} placeholder="kiriox" onChange={event => setConnection({ ...connection, database: event.target.value })} /></label><label>Espacio de datos<input value={connection.schema} placeholder="public" onChange={event => { setConnection({ ...connection, schema: event.target.value }); setSchema(event.target.value); }} /></label></div><label>Usuario<input required value={connection.username} onChange={event => setConnection({ ...connection, username: event.target.value })} /></label><label>Contraseña<input type="password" required value={connection.password} onChange={event => setConnection({ ...connection, password: event.target.value })} /></label><Button type="submit" disabled={busy === "connection"}>{busy === "connection" ? "Comprobando…" : "Comprobar conexión"}</Button></form></Card><Card className="explorer-card"><div className="section-heading"><div><span className="section-kicker">PASO 2 · ELEGIR DATOS</span><h2>Explora tus tablas</h2><p>{connectionId ? "Elige un espacio y una tabla para ver una muestra." : "Comprobar la conexión habilita esta sección."}</p></div>{loading && <LoadingState />}</div>{connectionId ? <div className="explorer-body"><label>Espacio de datos<select value={schema} onChange={event => setSchema(event.target.value)}>{schemas.map(item => <option key={item}>{item}</option>)}</select></label><label>Tabla que quieres revisar<select value={table} onChange={event => void inspectTable(event.target.value)}><option value="">Selecciona una tabla</option>{tables.map(item => <option key={item.table_name} value={item.table_name}>{item.table_name}</option>)}</select></label>{columns.length > 0 && <div className="column-list"><div className="list-heading"><b>Campos encontrados</b><span>{columns.length}</span></div>{columns.map(column => <div className="column-item" key={column.column_name}><span><Table2 size={14} />{column.column_name}</span><small>{column.is_nullable === "YES" ? "Puede quedar vacío" : "Necesario"}</small></div>)}</div>}<div className="action-row"><Button variant="secondary" disabled={!table} onClick={previewTable} icon={<Eye size={15} />}>Ver una muestra</Button><Button disabled={!table} onClick={createDatabaseSource} icon={<Plus size={15} />}>Agregar como fuente</Button></div>{rows.length > 0 && <PreviewPanel preview={{ columns: Object.keys(rows[0]), preview: rows as Record<string, string | null>[], rows_estimated: rows.length, inferred_types: Object.fromEntries(Object.keys(rows[0]).map(key => [key, "discovered"])) }} />}</div> : <EmptyState title="Aún no hay conexión" description="Completa el paso 1 y aquí podrás elegir la tabla que quieres cuidar." />}</Card></div></>;
}

function HistoryView({ sources, runs, selected, selectSource, loadExceptions, loadRunDetail, runDetail, clearRunDetail }: any) { return <><PageHeader eyebrow="LO QUE HA PASADO" title="Actividad" description="Aquí puedes ver qué ocurrió cada vez que revisaste una fuente." action={<label className="filter-control">Fuente<select value={selected?.id || ""} onChange={event => { const source = sources.find((item: Source) => item.id === event.target.value); if (source) selectSource(source); }}><option value="">Selecciona una fuente</option>{sources.map((source: Source) => <option key={source.id} value={source.id}>{source.name}</option>)}</select></label>} />{runs.length ? <Card className="history-card"><div className="metrics"><MetricCard label="Registros revisados" value={runs[0]?.rows_read ?? 0} /><MetricCard label="Nuevos" value={runs[0]?.rows_inserted ?? 0} tone="primary" /><MetricCard label="Por revisar" value={runs[0]?.rows_rejected ?? 0} tone="error" /><MetricCard label="Resultado" value={runs[0]?.status ?? "—"} /></div><DataTable><div className="data-row data-head history-grid"><span>Identificador</span><span>Fecha</span><span>Registros</span><span>Resultado</span><span>Acciones</span></div>{runs.map((run: Run) => <div className="data-row history-grid" key={run.id}><span className="mono">{run.id.slice(0, 8)}…</span><span>{new Date(run.started_at).toLocaleString("es-DO")}</span><span>{run.rows_read}</span><span><StatusBadge status={run.status} /></span><span className="history-actions"><Button variant="ghost" onClick={() => loadRunDetail(run)}>Ver detalles</Button><Button variant="ghost" onClick={() => loadExceptions(run)}>Ver problemas</Button></span></div>)}</DataTable></Card> : <Card><EmptyState title="Aún no hay actividad" description="Selecciona una fuente y procesa un archivo para ver aquí su resultado." /></Card>}{runDetail && <RunDetailPanel detail={runDetail} close={clearRunDetail} />}</>; }

function RunDetailPanel({ detail, close }: { detail: RunDetail; close: () => void }) { return <Card className="run-detail-card"><div className="detail-header"><div><span className="section-kicker">DETALLE DE ACTIVIDAD</span><h2>{detail.source_name || "Procesamiento"}</h2><p><span className="mono">{detail.id}</span> · {new Date(detail.started_at).toLocaleString("es-DO")}</p></div><div className="detail-actions"><StatusBadge status={detail.status} /><Button variant="ghost" onClick={close}>Cerrar</Button></div></div><div className="metrics"><MetricCard label="Revisados" value={detail.rows_read} /><MetricCard label="Nuevos" value={detail.rows_inserted} tone="primary" /><MetricCard label="Por revisar" value={detail.rows_rejected} tone="error" /><MetricCard label="Avisos" value={detail.warning_count} /></div><div className="run-detail-grid"><div><span className="section-kicker">CAMBIO DETECTADO</span><p>Antes: {detail.watermark_before || "Primera revisión"}</p><p>Después: {detail.watermark_after || "Sin cambios"}</p></div><div><span className="section-kicker">MENSAJES</span>{detail.errors.length ? detail.errors.map(error => <p className="run-error" key={error.id}>{error.message}</p>) : <p className="muted">No hay mensajes pendientes.</p>}</div></div>{detail.exceptions.length > 0 && <DataTable className="run-exceptions"><div className="data-row data-head run-exception-grid"><span>Fila</span><span>Campo</span><span>Valor</span><span>Qué pasó</span><span>Registro</span></div>{detail.exceptions.map(exception => <div className="data-row run-exception-grid" key={String(exception.id)}><span>{String(exception.row_number)}</span><span>{String(exception.column)}</span><span>{String(exception.value ?? "—")}</span><span>{String(exception.message)}</span><code className="raw-record">{JSON.stringify(exception.raw_record)}</code></div>)}</DataTable>}<details className="configuration-snapshot"><summary>Ver información técnica</summary><pre>{JSON.stringify(detail.configuration_snapshot, null, 2)}</pre></details></Card>; }

function ExceptionsView({ exceptions }: { exceptions: Record<string, unknown>[] }) { const [query, setQuery] = useState(""); const filtered = exceptions.filter(item => JSON.stringify(item).toLowerCase().includes(query.toLowerCase())); return <><PageHeader eyebrow="CALIDAD DE DATOS" title="Datos por revisar" description="Encuentra rápidamente las filas que necesitan atención antes de volver a procesarlas." action={<label className="search-control"><Search size={15} /><input placeholder="Buscar por fila o campo" value={query} onChange={event => setQuery(event.target.value)} /></label>} />{filtered.length ? <Card><DataTable><div className="data-row data-head exception-grid"><span>Fila</span><span>Campo</span><span>Valor</span><span>Revisión</span><span>Qué pasó</span></div>{filtered.map((item, index) => <div className="data-row exception-grid" key={index}><span>{String(item.row_number)}</span><span>{String(item.column)}</span><span className="error-value">{String(item.value)}</span><span>{String(item.rule)}</span><span>{String(item.message || item.error_code)}</span></div>)}</DataTable></Card> : <Card><EmptyState title="No hay datos por revisar" description="Cuando una fila no cumpla una comprobación, aparecerá aquí para que puedas corregirla." /></Card>}</>; }

function ScheduleView({ sources, schedule, setSchedule, createSchedule, schedules, toggleSchedule }: any) { return <><PageHeader eyebrow="AUTOMATIZA LO REPETITIVO" title="Automatizaciones" description="Decide cuándo revisar una fuente y deja que la plataforma lo haga por ti." action={<span className="header-note"><CalendarClock size={16} /> Se ejecuta en este equipo</span>} /><div className="schedule-layout"><Card><div className="section-heading"><div><span className="section-kicker">PASO 1 · PROGRAMAR</span><h2>Nueva automatización</h2><p>Elige una fuente, una frecuencia y una hora. Nosotros nos encargamos del resto.</p></div></div><form className="form-body" onSubmit={createSchedule}><label>¿Qué fuente quieres revisar?<select required value={schedule.data_source_id} onChange={event => setSchedule({ ...schedule, data_source_id: event.target.value })}><option value="">Selecciona una fuente</option>{sources.map((source: Source) => <option key={source.id} value={source.id}>{source.name}</option>)}</select></label><div className="form-grid"><label>Cada cuánto<select value={schedule.schedule_type} onChange={event => setSchedule({ ...schedule, schedule_type: event.target.value })}><option value="DAILY">Cada día</option><option value="HOURLY">Cada hora</option><option value="EVERY_X_HOURS">Cada ciertas horas</option><option value="WEEKLY">Cada semana</option><option value="MONTHLY">Cada mes</option><option value="MANUAL">Solo cuando yo lo pida</option></select></label><label>Hora local<input type="number" min="0" max="23" value={schedule.hour} onChange={event => setSchedule({ ...schedule, hour: Number(event.target.value) })} /></label></div><label>Zona horaria<input value={schedule.timezone} onChange={event => setSchedule({ ...schedule, timezone: event.target.value })} /><small>Usaremos la hora local de este equipo.</small></label><Button type="submit">Guardar automatización</Button></form></Card><Card className="schedule-list"><div className="section-heading"><div><span className="section-kicker">TUS AUTOMATIZACIONES</span><h2>Programaciones guardadas</h2><p>{schedules.length} activas en este equipo.</p></div></div>{schedules.length ? schedules.map((item: Record<string, unknown>, index: number) => <div className="schedule-item" key={String(item.id || index)}><StatusBadge status={item.enabled === false ? "INACTIVE" : String(item.schedule_type)} /><span>{item.next_run_at ? `Próxima revisión: ${new Date(String(item.next_run_at)).toLocaleString("es-DO")}` : "Cuando tú la pidas"}</span><Button type="button" variant="ghost" onClick={() => void toggleSchedule(String(item.id))}>{item.enabled === false ? "Activar" : "Pausar"}</Button></div>) : <EmptyState title="Aún no hay automatizaciones" description="Guarda una programación para no tener que repetir el proceso manualmente." />}</Card></div></>; }
